package com.googlecode.array4j;

// Adaptor for matrices to do things like: give me the first N rows/M columns. needed because we can only do matrix->vector with single stride.
public class FloatSubMatrix {
    
}
