package net.lunglet.spro;

public enum SProSignalFormat {
    /** RAW (linear 16) signal format. */
    PCM16,
    /** WAVE COMP360 signal format. */
    WAVE,
    /** SHERE signal format. */
    SPHERE
}
